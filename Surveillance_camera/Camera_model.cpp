#include "Camera_model.h"


Camera_model::Camera_model(string ID, string address_IP, string login, string password, string model) : Camera(ID, address_IP, login, password, model)
{
}


Camera_model::~Camera_model()
{
}
